<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="role-permissions-1" author="alphonce">
        <createTable tableName="role_permissions">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="role_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="permission_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="created_by" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign key to users.id -->
        <addForeignKeyConstraint
                baseTableName="role_permissions"
                baseColumnNames="role_id"
                constraintName="fk_role_permissions_roles"
                referencedTableName="roles"
                referencedColumnNames="id"/>

        <!-- Foreign key to roles.id -->
        <addForeignKeyConstraint
                baseTableName="role_permissions"
                baseColumnNames="permission_id"
                constraintName="fk_role_permissions_permissions"
                referencedTableName="permissions"
                referencedColumnNames="id"/>

        <!-- Foreign key to users.id for created_by -->
        <addForeignKeyConstraint
                baseTableName="role_permissions"
                baseColumnNames="created_by"
                constraintName="fk_role_permissions_users_created_by"
                referencedTableName="users"
                referencedColumnNames="id"/>

        <addUniqueConstraint
                tableName="role_permissions"
                columnNames="role_id,permission_id"
                constraintName="uk_role_permission"/>
    </changeSet>

    <changeSet id="insert-role-permissions-for-role-1" author="alphonce">
        <sql>
            INSERT INTO role_permissions (role_id, permission_id, created_by, created_at)
            SELECT 1, p.id, 1, NOW()
            FROM permissions p;
        </sql>
    </changeSet>

</databaseChangeLog>